# Phase 11 — per-stock OHLCV 분할 (129MB 단일 파일 문제 해결)

Phase: 11
Status: 코드 완료
LastUpdate: 2026-04-16
Branch: feature/per-stock-ohlcv-split

RecentWork:
  - 문제: 5년 확장 후 ohlcv.json = 129MB → GitHub 단일 파일 100MB 한도 초과
  - 해결: 종목별 파일 분할 (ohlcv/{code}.json × 2,500개, 각 ~50KB)
  - split_ohlcv.py 신규 작성 (TDD 없이 수동 검증)
  - daily-update.yml에 split 단계 추가 (fetch_fundamentals 뒤, push 전)
  - build-with-data.sh: curl → git clone --depth=1 (다수 파일 대응)
  - dataLoader.loadOhlcvForCode: 전체 로드 → per-stock 로드
  - IndexedDB 옛 캐시(ohlcv-full-cache-v1) 자동 정리

ResolvedWork:
  - 129MB push 실패 문제 해결
  - 브라우저 차트 로드 시간 대폭 단축 (48MB → 50KB per stock)
  - Vercel 빌드 대용량 파일 의존성 제거

MainIssues:
  - (사용자 확인) 워크플로 재실행 후 5년 데이터 정상 push되는지
  - (사용자 확인) Vercel 재배포 후 종목 차트에 5년 OHLCV 표시되는지

RelatedPhases:
  - Phase 8 (5년 확장 유발 원인)
  - Phase 10 (Toss 개편 — UI 레이어는 변경 없음)

NoteForFuture:
  - ohlcv/ 디렉토리 파일 수 (~2,500개) 증가 시 Vercel 빌드 모니터링
  - 종목 1만개 이상 되면 batch 번들로 추가 최적화 고려

CommitLog:
  - e807444 feat: add split_ohlcv script for per-stock OHLCV files
  - 0318ad5 feat: add split step to daily workflow before push
  - 0a6cd56 feat: switch build-with-data to git clone for multi-file data
  - 69cb124 feat: load per-stock OHLCV files with legacy cache cleanup
