# Phase 2 — 인증 시스템 & 미들웨어 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 비밀 URL + ID/PW 로그인 시스템을 구축한다. 루트 접근은 404, 비밀 경로는 JWT 쿠키 검증 후 통과.

**Architecture:** Next.js 14 Middleware(Edge runtime)가 모든 요청에서 ① SITE_ENABLED 확인 ② 비밀 경로 검증 ③ JWT 쿠키 검증 수행. 로그인 API는 bcrypt로 비번 검증 후 jose로 JWT 서명하여 httpOnly 쿠키 발급.

**Tech Stack:** Next.js 14 + bcryptjs + jose (JWT) + Vitest (테스트)

**Branch:** `feature/phase-2-auth` (Phase 1 머지 후 새로 생성)

**Prerequisite:** Phase 1 완료 (프로젝트 스캐폴딩 + TypeScript 타입 + 한글 리소스)

---

## 📁 File Structure

### 생성 파일

**인증 핵심 로직**
- `src/lib/auth/password.ts` — bcrypt 비교
- `src/lib/auth/jwt.ts` — JWT 생성·검증 (jose)
- `src/lib/auth/rateLimit.ts` — 로그인 시도 제한 (in-memory)
- `src/lib/auth/config.ts` — env 변수 검증 유틸

**테스트**
- `src/lib/auth/__tests__/password.test.ts`
- `src/lib/auth/__tests__/jwt.test.ts`
- `src/lib/auth/__tests__/rateLimit.test.ts`

**API 라우트**
- `src/app/api/auth/login/route.ts`
- `src/app/api/auth/logout/route.ts`

**미들웨어**
- `src/middleware.ts`

**비밀 경로 페이지**
- `src/app/[basePath]/layout.tsx` — env 일치 검증
- `src/app/[basePath]/login/page.tsx` — Server Component
- `src/app/[basePath]/login/LoginForm.tsx` — Client Component

**유틸 스크립트**
- `scripts/hash-password.ts` — 비번 해시 생성

**문자열 확장**
- `src/lib/strings/ko.ts` — auth 관련 추가 (이미 Phase 1에서 일부 포함, 필요 시 추가)

**설정**
- `vitest.config.ts`
- `package.json` 수정 — 의존성 + test 스크립트

---

## Task 1: 브랜치 생성 & 의존성 추가

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`

- [ ] **Step 1: Phase 2 브랜치 생성**

```bash
cd c:/Users/rk454/Desktop/Project/Money/MoneyProject
# Phase 1 머지된 상태 가정. main에서 분기.
git checkout main
git pull origin main
git checkout -b feature/phase-2-auth
```

- [ ] **Step 2: 의존성 설치**

```bash
npm install bcryptjs@^2.4.3 jose@^5.6.3
npm install --save-dev @types/bcryptjs@^2.4.6 vitest@^1.6.0 @vitest/ui@^1.6.0 happy-dom@^14.12.0 tsx@^4.15.0
```

- [ ] **Step 3: package.json scripts 추가**

기존 scripts에 추가:
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "hash-password": "tsx scripts/hash-password.ts"
  }
}
```

- [ ] **Step 4: vitest.config.ts 작성**

```typescript
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    environment: 'happy-dom',
    globals: true,
    include: ['src/**/*.{test,spec}.{ts,tsx}']
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, 'src') }
  }
})
```

- [ ] **Step 5: 설치 확인**

```bash
npm run test -- --version
```

Expected: `vitest/1.6.x` 출력.

- [ ] **Step 6: 커밋**

```bash
git add package.json package-lock.json vitest.config.ts
git commit -m "feat(phase-2): add auth dependencies and vitest config"
```

---

## Task 2: 비번 해시 생성 유틸 스크립트

**Files:**
- Create: `scripts/hash-password.ts`

- [ ] **Step 1: scripts/hash-password.ts 작성**

```typescript
#!/usr/bin/env tsx
/**
 * 비밀번호 해시 생성 유틸.
 * 사용: npm run hash-password -- "내비번"
 * 출력: bcrypt 해시 (cost factor 12)
 */
import bcrypt from 'bcryptjs'

async function main() {
  const password = process.argv[2]
  if (!password) {
    console.error('Usage: npm run hash-password -- "<password>"')
    process.exit(1)
  }
  if (password.length < 8) {
    console.error('Password must be at least 8 characters.')
    process.exit(1)
  }
  const hash = await bcrypt.hash(password, 12)
  console.log(hash)
}

main().catch((e) => {
  console.error(e)
  process.exit(1)
})
```

- [ ] **Step 2: 실행 검증**

```bash
npm run hash-password -- "testpassword123"
```

Expected: `$2b$12$...` (60자 bcrypt 해시) 출력.

- [ ] **Step 3: 커밋**

```bash
git add scripts/hash-password.ts
git commit -m "feat(phase-2): add password hash generator script"
```

---

## Task 3: password.ts (TDD)

**Files:**
- Create: `src/lib/auth/password.ts`, `src/lib/auth/__tests__/password.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// src/lib/auth/__tests__/password.test.ts
import { describe, it, expect } from 'vitest'
import bcrypt from 'bcryptjs'
import { verifyPassword } from '../password'

describe('verifyPassword', () => {
  it('returns true for correct password', async () => {
    const hash = await bcrypt.hash('correct_password', 12)
    const result = await verifyPassword('correct_password', hash)
    expect(result).toBe(true)
  })

  it('returns false for wrong password', async () => {
    const hash = await bcrypt.hash('correct_password', 12)
    const result = await verifyPassword('wrong_password', hash)
    expect(result).toBe(false)
  })

  it('returns false for empty password', async () => {
    const hash = await bcrypt.hash('correct_password', 12)
    const result = await verifyPassword('', hash)
    expect(result).toBe(false)
  })

  it('returns false for invalid hash', async () => {
    const result = await verifyPassword('anything', 'not_a_valid_hash')
    expect(result).toBe(false)
  })
})
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npm test src/lib/auth/__tests__/password.test.ts
```

Expected: FAIL — `../password` 없음.

- [ ] **Step 3: src/lib/auth/password.ts 구현**

```typescript
import bcrypt from 'bcryptjs'

export async function verifyPassword(plain: string, hash: string): Promise<boolean> {
  if (!plain || !hash) return false
  try {
    return await bcrypt.compare(plain, hash)
  } catch {
    return false
  }
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npm test src/lib/auth/__tests__/password.test.ts
```

Expected: 4개 PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/lib/auth/password.ts src/lib/auth/__tests__/password.test.ts
git commit -m "feat(phase-2): add password verification utility"
```

---

## Task 4: jwt.ts (TDD)

**Files:**
- Create: `src/lib/auth/jwt.ts`, `src/lib/auth/__tests__/jwt.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// src/lib/auth/__tests__/jwt.test.ts
import { describe, it, expect, beforeAll } from 'vitest'
import { signJWT, verifyJWT } from '../jwt'

const SECRET = 'test-secret-at-least-32-bytes-long-for-HS256-ok'

beforeAll(() => {
  process.env.JWT_SECRET = SECRET
})

describe('signJWT / verifyJWT', () => {
  it('sign and verify returns the same payload uid', async () => {
    const token = await signJWT({ uid: 'kyungwon' })
    const payload = await verifyJWT(token)
    expect(payload.uid).toBe('kyungwon')
  })

  it('rejects tampered token', async () => {
    const token = await signJWT({ uid: 'kyungwon' })
    const tampered = token.slice(0, -5) + 'aaaaa'
    await expect(verifyJWT(tampered)).rejects.toThrow()
  })

  it('rejects token signed with different secret', async () => {
    const token = await signJWT({ uid: 'kyungwon' })
    process.env.JWT_SECRET = 'different-secret-at-least-32-bytes-long-ok'
    await expect(verifyJWT(token)).rejects.toThrow()
    process.env.JWT_SECRET = SECRET
  })

  it('token has 7-day expiration', async () => {
    const before = Math.floor(Date.now() / 1000)
    const token = await signJWT({ uid: 'oh' })
    const payload = await verifyJWT(token)
    const expectedExp = before + 60 * 60 * 24 * 7
    expect(payload.exp).toBeGreaterThanOrEqual(expectedExp - 5)
    expect(payload.exp).toBeLessThanOrEqual(expectedExp + 5)
  })
})
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npm test src/lib/auth/__tests__/jwt.test.ts
```

Expected: FAIL.

- [ ] **Step 3: src/lib/auth/jwt.ts 구현**

```typescript
import { SignJWT, jwtVerify } from 'jose'

const JWT_ALG = 'HS256'
const EXPIRES_SEC = 60 * 60 * 24 * 7  // 7 days

export interface JWTPayload {
  uid: string
  iat?: number
  exp?: number
}

function getSecret(): Uint8Array {
  const s = process.env.JWT_SECRET
  if (!s || s.length < 32) {
    throw new Error('JWT_SECRET must be at least 32 bytes')
  }
  return new TextEncoder().encode(s)
}

export async function signJWT(payload: Pick<JWTPayload, 'uid'>): Promise<string> {
  const now = Math.floor(Date.now() / 1000)
  return await new SignJWT({ uid: payload.uid })
    .setProtectedHeader({ alg: JWT_ALG })
    .setIssuedAt(now)
    .setExpirationTime(now + EXPIRES_SEC)
    .sign(getSecret())
}

export async function verifyJWT(token: string): Promise<JWTPayload> {
  const { payload } = await jwtVerify(token, getSecret(), {
    algorithms: [JWT_ALG]
  })
  if (typeof payload.uid !== 'string') {
    throw new Error('Invalid JWT payload')
  }
  return payload as JWTPayload
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npm test src/lib/auth/__tests__/jwt.test.ts
```

Expected: 4개 PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/lib/auth/jwt.ts src/lib/auth/__tests__/jwt.test.ts
git commit -m "feat(phase-2): add JWT sign/verify (jose, HS256, 7-day)"
```

---

## Task 5: rateLimit.ts (TDD)

**Files:**
- Create: `src/lib/auth/rateLimit.ts`, `src/lib/auth/__tests__/rateLimit.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// src/lib/auth/__tests__/rateLimit.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { checkRateLimit, _resetRateLimitStore } from '../rateLimit'

describe('checkRateLimit', () => {
  beforeEach(() => {
    _resetRateLimitStore()
    vi.useRealTimers()
  })

  it('allows first 5 attempts', () => {
    for (let i = 0; i < 5; i++) {
      expect(checkRateLimit('1.2.3.4')).toBe(true)
    }
  })

  it('blocks 6th attempt within window', () => {
    for (let i = 0; i < 5; i++) checkRateLimit('1.2.3.4')
    expect(checkRateLimit('1.2.3.4')).toBe(false)
  })

  it('allows different IPs independently', () => {
    for (let i = 0; i < 5; i++) checkRateLimit('1.2.3.4')
    expect(checkRateLimit('5.6.7.8')).toBe(true)
  })

  it('resets after 15 minute window', () => {
    vi.useFakeTimers()
    const now = Date.now()
    vi.setSystemTime(now)

    for (let i = 0; i < 5; i++) checkRateLimit('1.2.3.4')
    expect(checkRateLimit('1.2.3.4')).toBe(false)

    vi.setSystemTime(now + 15 * 60 * 1000 + 1000)
    expect(checkRateLimit('1.2.3.4')).toBe(true)
  })
})
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npm test src/lib/auth/__tests__/rateLimit.test.ts
```

Expected: FAIL.

- [ ] **Step 3: src/lib/auth/rateLimit.ts 구현**

```typescript
const MAX_ATTEMPTS = 5
const WINDOW_MS = 15 * 60 * 1000

interface Entry {
  count: number
  resetAt: number
}

const store = new Map<string, Entry>()

export function checkRateLimit(ip: string): boolean {
  const now = Date.now()
  const entry = store.get(ip)

  if (!entry || now >= entry.resetAt) {
    store.set(ip, { count: 1, resetAt: now + WINDOW_MS })
    return true
  }

  if (entry.count >= MAX_ATTEMPTS) return false

  entry.count += 1
  return true
}

/** Test-only helper */
export function _resetRateLimitStore(): void {
  store.clear()
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npm test src/lib/auth/__tests__/rateLimit.test.ts
```

Expected: 4개 PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/lib/auth/rateLimit.ts src/lib/auth/__tests__/rateLimit.test.ts
git commit -m "feat(phase-2): add in-memory rate limiter (5 attempts / 15 min)"
```

---

## Task 6: auth config 유틸 (env 검증)

**Files:**
- Create: `src/lib/auth/config.ts`

- [ ] **Step 1: src/lib/auth/config.ts 작성**

```typescript
/**
 * 환경 변수에서 등록된 사용자 정보를 읽고 검증.
 * USER_<KEY>_ID, USER_<KEY>_HASH 쌍을 찾아 반환.
 */
export interface RegisteredUser {
  id: string
  hash: string
  key: string   // env에서의 고유 키 (예: KYUNGWON, OH)
}

export function getRegisteredUsers(): RegisteredUser[] {
  const users: RegisteredUser[] = []
  for (const envKey of Object.keys(process.env)) {
    const match = envKey.match(/^USER_(.+)_ID$/)
    if (!match) continue
    const key = match[1]
    const id = process.env[envKey]
    const hash = process.env[`USER_${key}_HASH`]
    if (!id || !hash) continue
    users.push({ id, hash, key })
  }
  return users
}

export function findUserById(id: string): RegisteredUser | undefined {
  return getRegisteredUsers().find((u) => u.id === id)
}

export function getSecretBasePath(): string {
  const p = process.env.SECRET_BASE_PATH
  if (!p) throw new Error('SECRET_BASE_PATH env is required')
  return p
}

export function isSiteEnabled(): boolean {
  return process.env.SITE_ENABLED !== 'false'
}
```

- [ ] **Step 2: TypeScript 컴파일 검증**

```bash
npm run type-check
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add src/lib/auth/config.ts
git commit -m "feat(phase-2): add auth config reader for env users"
```

---

## Task 7: Login API 라우트

**Files:**
- Create: `src/app/api/auth/login/route.ts`

- [ ] **Step 1: src/app/api/auth/login/route.ts 작성**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { findUserById, getSecretBasePath } from '@/lib/auth/config'
import { verifyPassword } from '@/lib/auth/password'
import { signJWT } from '@/lib/auth/jwt'
import { checkRateLimit } from '@/lib/auth/rateLimit'
import { strings } from '@/lib/strings/ko'

const COOKIE_NAME = 'auth_token'
const COOKIE_MAX_AGE = 60 * 60 * 24 * 7  // 7 days

export async function POST(req: NextRequest) {
  // Rate limit by IP
  const ip = req.headers.get('x-forwarded-for')?.split(',')[0]?.trim()
           || req.headers.get('x-real-ip')
           || 'unknown'
  if (!checkRateLimit(ip)) {
    return NextResponse.json(
      { error: strings.auth.rateLimit },
      { status: 429 }
    )
  }

  let body: { id?: string, pw?: string, agree?: boolean }
  try {
    body = await req.json()
  } catch {
    return NextResponse.json(
      { error: strings.auth.loginFail },
      { status: 400 }
    )
  }

  if (!body.agree) {
    return NextResponse.json(
      { error: strings.auth.agreementRequired },
      { status: 400 }
    )
  }

  if (!body.id || !body.pw) {
    return NextResponse.json(
      { error: strings.auth.loginFail },
      { status: 401 }
    )
  }

  const user = findUserById(body.id)
  if (!user) {
    return NextResponse.json(
      { error: strings.auth.loginFail },
      { status: 401 }
    )
  }

  const ok = await verifyPassword(body.pw, user.hash)
  if (!ok) {
    return NextResponse.json(
      { error: strings.auth.loginFail },
      { status: 401 }
    )
  }

  const token = await signJWT({ uid: user.id })
  const basePath = getSecretBasePath()

  const res = NextResponse.json({
    ok: true,
    redirect: `/${basePath}/screener`
  })

  res.cookies.set(COOKIE_NAME, token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: COOKIE_MAX_AGE,
    path: '/'
  })

  return res
}
```

- [ ] **Step 2: TypeScript 컴파일 검증**

```bash
npm run type-check
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add src/app/api/auth/login/route.ts
git commit -m "feat(phase-2): add login API route (bcrypt + JWT + rate limit)"
```

---

## Task 8: Logout API 라우트

**Files:**
- Create: `src/app/api/auth/logout/route.ts`

- [ ] **Step 1: src/app/api/auth/logout/route.ts 작성**

```typescript
import { NextResponse } from 'next/server'
import { getSecretBasePath } from '@/lib/auth/config'

export async function POST() {
  const basePath = getSecretBasePath()
  const res = NextResponse.json({ ok: true, redirect: `/${basePath}/login` })
  res.cookies.set('auth_token', '', {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 0,
    path: '/'
  })
  return res
}
```

- [ ] **Step 2: 컴파일 검증**

```bash
npm run type-check
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add src/app/api/auth/logout/route.ts
git commit -m "feat(phase-2): add logout API route"
```

---

## Task 9: Middleware (비밀 경로 + JWT + SITE_ENABLED)

**Files:**
- Create: `src/middleware.ts`

- [ ] **Step 1: src/middleware.ts 작성**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { jwtVerify } from 'jose'

const PUBLIC_PATHS = ['/robots.txt', '/favicon.ico']

function getSecret(): Uint8Array {
  const s = process.env.JWT_SECRET
  if (!s) throw new Error('JWT_SECRET required')
  return new TextEncoder().encode(s)
}

function maintenanceResponse(): NextResponse {
  const html = `<!DOCTYPE html>
<html lang="ko"><head><meta charset="utf-8">
<title>Service Unavailable</title>
<meta name="robots" content="noindex, nofollow">
<style>
body { font-family: system-ui, -apple-system, sans-serif; background:#141414; color:#f5f5f5; 
  display:flex; align-items:center; justify-content:center; min-height:100vh; margin:0; }
.box { text-align:center; max-width:500px; padding:40px; }
h1 { font-size:24px; margin-bottom:16px; }
p  { color:#aaa; line-height:1.6; }
</style></head><body>
<div class="box">
<h1>🛠️ 서비스 일시 중단 중입니다</h1>
<p>이번 달 무료 사용 한도에 근접하여 서비스가 일시 중단되었습니다.</p>
<p>곧 재개 예정입니다.</p>
</div></body></html>`
  return new NextResponse(html, {
    status: 503,
    headers: {
      'Content-Type': 'text/html; charset=utf-8',
      'Cache-Control': 'no-store',
      'Retry-After': '86400'
    }
  })
}

export async function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl
  const basePath = process.env.SECRET_BASE_PATH || ''

  // ① SITE_ENABLED 체크
  if (process.env.SITE_ENABLED === 'false') {
    return maintenanceResponse()
  }

  // ② PUBLIC 파일 통과
  if (PUBLIC_PATHS.some((p) => pathname === p)) {
    return NextResponse.next()
  }

  // ③ /data/* 는 (인증 없이) 정적 파일 서빙 통과
  if (pathname.startsWith('/data/')) {
    return NextResponse.next()
  }

  // ④ /api/auth/* 는 미들웨어 통과 (API 내부에서 처리)
  if (pathname.startsWith('/api/auth/')) {
    return NextResponse.next()
  }

  // ⑤ 루트 또는 비밀 경로 외 → 404
  if (!basePath || !pathname.startsWith(`/${basePath}`)) {
    return new NextResponse('Not Found', { status: 404 })
  }

  // ⑥ 로그인 페이지는 인증 불필요
  if (pathname === `/${basePath}/login`) {
    return NextResponse.next()
  }

  // ⑦ JWT 쿠키 검증
  const token = req.cookies.get('auth_token')?.value
  if (!token) {
    return NextResponse.redirect(new URL(`/${basePath}/login`, req.url))
  }

  try {
    await jwtVerify(token, getSecret(), { algorithms: ['HS256'] })
    return NextResponse.next()
  } catch {
    return NextResponse.redirect(new URL(`/${basePath}/login`, req.url))
  }
}

export const config = {
  matcher: ['/((?!_next/static|_next/image).*)']
}
```

- [ ] **Step 2: 컴파일 검증**

```bash
npm run type-check
```

Expected: 에러 없음.

- [ ] **Step 3: 로컬 dev 서버에서 수동 검증 (일부)**

```bash
# .env.local 작성 필요:
#   JWT_SECRET=<32자 이상>
#   SECRET_BASE_PATH=dev
#   USER_KYUNGWON_ID=kyungwon
#   USER_KYUNGWON_HASH=<npm run hash-password -- "test1234" 결과>
#   SITE_ENABLED=true

npm run dev
```

- Browser: `http://localhost:3000/` → 404 표시
- Browser: `http://localhost:3000/wrongpath` → 404
- Browser: `http://localhost:3000/dev/screener` → `/dev/login` 으로 리다이렉트

- [ ] **Step 4: 커밋**

```bash
git add src/middleware.ts
git commit -m "feat(phase-2): add middleware (secret path + JWT + SITE_ENABLED)"
```

---

## Task 10: [basePath] 레이아웃 (env 검증)

**Files:**
- Create: `src/app/[basePath]/layout.tsx`

- [ ] **Step 1: src/app/[basePath]/layout.tsx 작성**

```tsx
import { notFound } from 'next/navigation'

export default function SecretLayout({
  children,
  params
}: {
  children: React.ReactNode
  params: { basePath: string }
}) {
  const expected = process.env.SECRET_BASE_PATH
  if (!expected || params.basePath !== expected) {
    notFound()
  }
  return <>{children}</>
}
```

**참고**: 이 레이아웃은 미들웨어와 중복 검증처럼 보이지만, 미들웨어가 Edge에서 최초 거름망 역할을 하고, 레이아웃은 React 렌더 트리에서 재보증하는 이중 방어선입니다.

- [ ] **Step 2: 컴파일 검증**

```bash
npm run type-check
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add src/app/[basePath]/layout.tsx
git commit -m "feat(phase-2): add secret base path layout guard"
```

---

## Task 11: Login Page + LoginForm Client Component

**Files:**
- Create: `src/app/[basePath]/login/page.tsx`, `src/app/[basePath]/login/LoginForm.tsx`

- [ ] **Step 1: src/app/[basePath]/login/page.tsx 작성 (Server Component)**

```tsx
import { strings } from '@/lib/strings/ko'
import LoginForm from './LoginForm'

export default function LoginPage() {
  return (
    <main className="min-h-screen flex items-center justify-center pb-[120px] px-4">
      <div className="w-full max-w-sm">
        <h1 className="text-2xl font-bold mb-6 text-center">
          {strings.auth.loginTitle}
        </h1>

        <LoginForm />

        <footer className="mt-8 text-xs text-text-secondary-light dark:text-text-secondary-dark leading-relaxed">
          {strings.legal.disclaimer}
        </footer>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: src/app/[basePath]/login/LoginForm.tsx 작성 (Client Component)**

```tsx
'use client'

import { useState } from 'react'
import { strings } from '@/lib/strings/ko'

export default function LoginForm() {
  const [id, setId] = useState('')
  const [pw, setPw] = useState('')
  const [agree, setAgree] = useState(false)
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  async function submit(e: React.FormEvent) {
    e.preventDefault()
    setError('')

    if (!agree) {
      setError(strings.auth.agreementRequired)
      return
    }

    setLoading(true)
    try {
      const res = await fetch('/api/auth/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id, pw, agree })
      })
      const data = await res.json()
      if (!res.ok) {
        setError(data.error || strings.auth.loginFail)
        return
      }
      window.location.href = data.redirect
    } catch {
      setError(strings.common.error)
    } finally {
      setLoading(false)
    }
  }

  return (
    <form onSubmit={submit} className="space-y-4">
      <div>
        <label htmlFor="id" className="block text-sm font-medium mb-1">
          {strings.auth.idLabel}
        </label>
        <input
          id="id"
          name="id"
          type="text"
          autoComplete="username"
          required
          value={id}
          onChange={(e) => setId(e.target.value)}
          className="w-full px-3 py-2 border border-border-light dark:border-border-dark rounded-md bg-bg-primary-light dark:bg-bg-secondary-dark"
        />
      </div>

      <div>
        <label htmlFor="pw" className="block text-sm font-medium mb-1">
          {strings.auth.pwLabel}
        </label>
        <input
          id="pw"
          name="pw"
          type="password"
          autoComplete="current-password"
          required
          value={pw}
          onChange={(e) => setPw(e.target.value)}
          className="w-full px-3 py-2 border border-border-light dark:border-border-dark rounded-md bg-bg-primary-light dark:bg-bg-secondary-dark"
        />
      </div>

      <label className="flex items-start gap-2 text-xs">
        <input
          type="checkbox"
          checked={agree}
          onChange={(e) => setAgree(e.target.checked)}
          className="mt-0.5"
        />
        <span>{strings.auth.agreementLabel}</span>
      </label>

      {error && (
        <p role="alert" className="text-sm text-positive-light dark:text-positive-dark">
          {error}
        </p>
      )}

      <button
        type="submit"
        disabled={loading}
        className="w-full py-2 bg-accent-light dark:bg-accent-dark text-white rounded-md font-medium disabled:opacity-50"
      >
        {loading ? strings.common.loading : strings.auth.loginButton}
      </button>
    </form>
  )
}
```

- [ ] **Step 3: Tailwind config에 border 색상 토큰 추가**

`tailwind.config.ts`의 `colors.extend`에 추가:
```typescript
colors: {
  // ... 기존
  border: { light: '#E4E4E4', dark: '#333333' }
}
```

- [ ] **Step 4: 빌드 검증**

```bash
npm run type-check
npm run build
```

Expected: 에러 없음.

- [ ] **Step 5: 커밋**

```bash
git add src/app/[basePath]/login/ tailwind.config.ts
git commit -m "feat(phase-2): add login page and form with disclaimer checkbox"
```

---

## Task 12: E2E 수동 검증

**Files:** 없음 (검증만)

- [ ] **Step 1: .env.local 셋업**

```
JWT_SECRET=dev-secret-at-least-32-bytes-long-for-HS256-ok
SECRET_BASE_PATH=dev
USER_KYUNGWON_ID=kyungwon
# 아래 해시는 "test1234" 의 예시. 실제론 npm run hash-password로 생성
USER_KYUNGWON_HASH=$2b$12$yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy
USER_OH_ID=oh
USER_OH_HASH=$2b$12$zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzz
SITE_ENABLED=true
```

- [ ] **Step 2: 유효한 해시 생성 후 env 반영**

```bash
npm run hash-password -- "test1234"
# 출력 해시를 USER_KYUNGWON_HASH 값으로 교체
```

- [ ] **Step 3: 로컬 dev 실행**

```bash
npm run dev
```

- [ ] **Step 4: 시나리오별 검증**

| 시나리오 | URL | 예상 |
|---|---|---|
| 루트 접속 | http://localhost:3000/ | 404 |
| 잘못된 경로 | http://localhost:3000/wrong | 404 |
| 비밀 경로 로그인 | http://localhost:3000/dev/login | 로그인 폼 |
| 다른 비밀 경로 보호 확인 | http://localhost:3000/dev/screener | /dev/login 으로 리다이렉트 |
| 미동의 상태 로그인 시도 | (체크박스 off) | "약관에 동의해주세요" 에러 |
| 잘못된 비번 | 동의 O + `wrong` | "아이디 또는 비밀번호가 일치하지 않습니다" |
| 올바른 로그인 | 동의 O + `kyungwon` / `test1234` | /dev/screener 리다이렉트 (임시 404 OK — Phase 4까지 미구현) |
| 쿠키 확인 | DevTools → Application → Cookies | `auth_token` httpOnly 쿠키 존재 |
| 로그아웃 API | `curl -X POST http://localhost:3000/api/auth/logout -b cookies.txt -c cookies.txt` | 쿠키 삭제, ok: true |
| Rate Limit | 비번 6회 연속 틀림 | 6번째부터 429 응답 |
| SITE_ENABLED=false | .env 변경 후 서버 재시작 | 모든 경로 503 유지보수 페이지 |

- [ ] **Step 5: 전체 유닛 테스트 실행**

```bash
npm test
```

Expected: 전 테스트 PASS (Phase 1 Python 테스트는 별도 `pytest`로, 이건 Vitest용).

---

## Task 13: Workprogress 기록 & PR 준비

**Files:**
- Create: `Workprogress/Phase_2_2026-04-14.md`

- [ ] **Step 1: Workprogress 작성**

```markdown
# Phase 2 — 인증 시스템 & 미들웨어

Phase: 2
Status: 완료
LastUpdate: 2026-04-14
Branch: feature/phase-2-auth

RecentWork:
  - bcrypt + jose 기반 JWT 인증 구축
  - 비밀 URL (SECRET_BASE_PATH) 미들웨어 가드
  - 로그인 API (Rate Limit 포함)
  - 로그인 페이지 + 약관 동의 체크박스
  - 유지보수 페이지 (SITE_ENABLED=false 대응)
  - E2E 시나리오 12개 수동 검증 통과

MainWork:
  - (Phase 3으로) 공통 레이아웃 (헤더/푸터/모드토글/테마토글)

ResolvedWork:
  - password.ts / jwt.ts / rateLimit.ts (TDD)
  - Login / Logout API
  - middleware.ts
  - [basePath]/login 페이지
  - hash-password.ts 유틸

MainIssues:
  - 없음

ResolvedIssues:
  - 없음

RelatedPhases:
  - Phase 1 (스캐폴딩, 문자열)
  - Phase 3 (공통 레이아웃, 로그아웃 버튼)
  - Phase 6 (SITE_ENABLED 자동 제어)
```

- [ ] **Step 2: 커밋**

```bash
git add Workprogress/Phase_2_2026-04-14.md
git commit -m "docs(phase-2): add Phase 2 workprogress record"
```

- [ ] **Step 3: 사용자 확인 후 푸시 + PR 생성**

```bash
git push -u origin feature/phase-2-auth
```

- [ ] **Step 4: /requesting-code-review 실행**

- [ ] **Step 5: /verification-before-completion 실행**

---

## 📊 Self-Review

### Spec Coverage

| 스펙 항목 | Phase 2 반영 |
|---|---|
| 7.1 비밀 URL | ✅ middleware + [basePath]/layout |
| 7.2 사용자 등록 | ✅ config.ts (env 변수 읽기) |
| 7.3 bcrypt cost 12 | ✅ hash-password.ts |
| 7.4 로그인 플로우 | ✅ Login API + LoginForm |
| 7.5 JWT 구조 | ✅ jwt.ts (HS256, 7일) |
| 7.6 쿠키 속성 | ✅ httpOnly, secure, sameSite=lax |
| 7.7 미들웨어 흐름 | ✅ middleware.ts (6단계 체크) |
| 7.8 Rate Limiting | ✅ rateLimit.ts (5회/15분) |
| 7.9 크롤러 차단 | 🔄 robots.txt는 Phase 1, 메타태그는 Phase 3 layout에서 |
| 7.10 법적 고지 | ✅ LoginForm 체크박스 |

### Placeholder 스캔
- "TBD"/"TODO" 없음 ✅
- 모든 테스트·구현 코드 완전 ✅

### Type Consistency
- `JWTPayload.uid` - 모든 곳에서 일관 ✅
- `RegisteredUser` - config.ts 전역 ✅
- `COOKIE_NAME = 'auth_token'` - login/logout/middleware 일관 ✅

---

## 🏁 Phase 2 완료 조건

- [ ] 13개 Task 모두 체크
- [ ] Vitest 전 테스트 PASS (password/jwt/rateLimit 약 15 케이스)
- [ ] 12개 E2E 시나리오 수동 검증 통과
- [ ] `npm run build` 성공
- [ ] `feature/phase-2-auth` 브랜치 커밋
- [ ] Workprogress 기록
- [ ] 사용자 확인 후 푸시 + PR
- [ ] /requesting-code-review + /verification-before-completion 통과
